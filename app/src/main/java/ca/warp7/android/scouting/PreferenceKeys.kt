package ca.warp7.android.scouting

object PreferenceKeys {

    // the setting for the board
    const val kBoard = "pref_key_board"

    // the setting for the scout name
    const val kScout = "pref_key_scout"

    // the setting for the current team
    const val kTeam = "pref_key_team"

    // the setting for the current year
    const val kYear = "pref_key_year"

    // the setting for the current event key
    const val kEventKey = "pref_key_event_key"

    // the setting for the current event name
    const val kEventName = "pref_key_event_name"
}
<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_new_entry"
        android:title="@string/menu_select_specs"
        android:icon="@drawable/ic_add_ablack"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/delete_entries"
        android:icon="@drawable/ic_delete_sweep_ablack"
        android:title="@string/menu_select_specs"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_settings"
        android:title="@string/menu_select_specs"
        android:icon="@drawable/ic_settings_ablack"
        app:showAsAction="ifRoom"/>
</menu>
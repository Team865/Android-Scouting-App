<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_new_entry"
        android:title="@string/menu_new_entry"
        android:icon="@drawable/ic_add_ablack"
        app:showAsAction="ifRoom"
        android:visible="false"/>
    <item
        android:id="@+id/menu_toggle_scouted"
        android:title="@string/menu_toggle_scouted"
        android:icon="@drawable/ic_visibility_off_ablack"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/menu_settings"
        android:title="@string/menu_settings"
        android:icon="@drawable/ic_settings_ablack"
        app:showAsAction="ifRoom"/>
</menu>